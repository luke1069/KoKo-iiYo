<% if post.favorited_by?(current_user) %>
  <%= link_to post_favorites_path(post), method: :delete, class:"text-danger", remote: true do %>
    ♥<%= post.favorites.count %>
  <% end %>
<% else %>
  <%= link_to post_favorites_path(post), method: :post, remote: true do %>
    ♡<%= post.favorites.count %>
  <% end %>
<% end %>